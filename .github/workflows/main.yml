name: Enterprise Multi-Environment Deployment

on:
  push:
    branches:
      - main

env:
  IMAGE_NAME: python-app

jobs:

#################################################################################
# BUILD + TEST + SONAR
#################################################################################

  build-test-sonar:
    name: Build, Test & SonarQube
    runs-on: ubuntu-latest

    outputs:
      image_tag: ${{ steps.tag.outputs.tag }}

    steps:

      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install coverage

      - name: Run Unit Tests with Coverage
        run: |
          coverage run -m unittest discover -s tests
          coverage xml
          coverage report

      - name: Generate Image Tag
        id: tag
        run: |
          echo "tag=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
            -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }}
            -Dsonar.qualitygate.wait=true

#################################################################################
# BUILD DOCKER IMAGE
#################################################################################

  build-and-push:
    name: Build & Push Docker Image
    needs: build-test-sonar
    runs-on: ubuntu-latest

    outputs:
      image_tag: ${{ needs.build-test-sonar.outputs.image_tag }}

    steps:

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: |
          docker build \
            -t ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:${{ needs.build-test-sonar.outputs.image_tag }} .

      - name: Push Docker Image
        run: |
          docker push \
            ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:${{ needs.build-test-sonar.outputs.image_tag }}

#################################################################################
# DEPLOY DEV
#################################################################################

  deploy-dev:
    name: Deploy to DEV
    runs-on: ubuntu-latest
    needs: build-and-push
    environment: DEV

    steps:

      - name: Deploy to DEV EC2
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}

          script: |
            IMAGE=${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:${{ needs.build-and-push.outputs.image_tag }}

            docker pull $IMAGE

            docker stop python-app || true
            docker rm python-app || true

            docker run -d \
              --name python-app \
              --restart always \
              --env-file ${{ secrets.ENV_FILE_PATH }} \
              -p 5000:5000 \
              $IMAGE

#################################################################################
# DEPLOY QA
#################################################################################

  deploy-qa:
    name: Deploy to QA
    runs-on: ubuntu-latest
    needs: deploy-dev
    environment: QA

    steps:

      - name: Deploy to QA EC2
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}

          script: |
            IMAGE=${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:${{ needs.build-and-push.outputs.image_tag }}

            docker pull $IMAGE

            docker stop python-app || true
            docker rm python-app || true

            docker run -d \
              --name python-app \
              --restart always \
              --env-file ${{ secrets.ENV_FILE_PATH }} \
              -p 5000:5000 \
              $IMAGE

#################################################################################
# DEPLOY PROD (Manual Approval)
#################################################################################

  deploy-prod:
    name: Deploy to PROD
    runs-on: ubuntu-latest
    needs: deploy-qa
    environment: PROD

    steps:

      - name: Deploy to PROD EC2
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}

          script: |
            IMAGE=${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:${{ needs.build-and-push.outputs.image_tag }}

            docker pull $IMAGE

            docker stop python-app || true
            docker rm python-app || true

            docker run -d \
              --name python-app \
              --restart always \
              --env-file ${{ secrets.ENV_FILE_PATH }} \
              -p 5000:5000 \
              $IMAGE